These files are in the gallery map: <br />
<br />
<table>
{% for picture in gallery %}
    {% if loop.index is divisibleby(3) %}
        <tr>
    {% endif %}
        <td>
            {{ picture.getFileName }}
        </td>
    {% if loop.index is divisibleby(3) %}
        </tr>
    {% endif %}
{% endfor %}
</table>
